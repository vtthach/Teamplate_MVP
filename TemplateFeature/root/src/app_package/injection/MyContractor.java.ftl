package ${packageName}.features.${featureName?lower_case}.injection;

import ${packageName}.common.presenter.BasePresenter;
import ${packageName}.common.presenter.BasePresenterView;

public interface ${featureName}Contractor {
    interface ${featureName}Presenter extends BasePresenter {
       
    }

    interface ${featureName}View extends BasePresenterView {
       
    }
}
