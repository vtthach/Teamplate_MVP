package ${packageName}.features.${featureName?lower_case}.api.model;

import ${packageName}.application.api.model.BaseResponse;

import java.util.List;

public class ${featureName}Response extends BaseResponse {
  
}
